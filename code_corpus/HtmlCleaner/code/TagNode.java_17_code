    public int getChildIndex(final HtmlNode child) {
        int index = 0;
        for (final Object curr : children) {
            if (curr == child) {
                return index;
            }
            index++;
        }
        return -1;
    }

