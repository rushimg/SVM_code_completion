    /**
     * Returns the time zone for the axis.
     *
     * @return The time zone (never <code>null</code>).
     *
     * @since 1.0.4
     *
     * @see #setTimeZone(TimeZone)
     */
