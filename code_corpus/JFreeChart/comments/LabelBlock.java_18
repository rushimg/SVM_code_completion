    /**
     * Sets the content alignment point.
     *
     * @param anchor  the anchor used to determine the alignment point (never
     *         <code>null</code>).
     *
     * @since 1.0.13
     */
