  public void subtract(E o) {
    Integer count = cset.get(o);
    if ( count != null ) {
      int c = count -1;
      if (c == 0) {
        cset.remove(o);
      }
      else {
        cset.put(o, c);
      }
    }
  }

