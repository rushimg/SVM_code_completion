  public void add(char ch) {
    if (b.length == i) {

      char[] new_b = new char[i+INC];
      for (int c = 0; c < i; c++) new_b[c] = b[c]; {
        b = new_b;
      }
    }
    b[i++] = ch;
  }

