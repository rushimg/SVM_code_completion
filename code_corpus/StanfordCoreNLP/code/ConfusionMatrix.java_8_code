  public Set<U> uniqueLabels() {
    HashSet<U> ret = new HashSet<U>();
    for (Pair<U, U> pair : confTable.keySet()) {
      ret.add(pair.first());
      ret.add(pair.second());
    }
    return ret;
  }
  
