      double cc = 1.0 / alpha;
      double dd = 1.0 / (1.0-alpha);
      while (true) {
        double xx = Math.pow(Math.random(), cc);
        double yy = xx + Math.pow(Math.random(), dd);
        if (yy <= 1.0) {
        }
      }
    } else {
