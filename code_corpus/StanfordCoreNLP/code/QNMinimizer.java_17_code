                        newPt, bestPt, endPt, stpMin, stpMax);
      }

      if (bracketed) {
        }
        width1 = width;
        width = Math.abs(endPt[a] - bestPt[a]);
      }

    } while (true);
  }


