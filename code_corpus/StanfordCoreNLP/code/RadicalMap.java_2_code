  public static char getRadical(char ch) {
    Character character = charsToRads.get(ch);
    if (character != null) {
      return character.charValue();
    } else {
      return '.';  // this might be changed to return the original char
    }
  }

