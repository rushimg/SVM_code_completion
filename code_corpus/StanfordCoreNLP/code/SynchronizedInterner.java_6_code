  public Set<T> internAll(Set<T> s) {
    synchronized(mutex) {
      return delegate.internAll(s);
    }
  }

  public int size() {
    synchronized(mutex) {
      return delegate.size();
    }
  }

