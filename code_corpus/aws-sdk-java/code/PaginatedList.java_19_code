                    boolean clearPreviousResults = iterationOnly;
                    moveNextResults(clearPreviousResults);
                } 
                
                if ( iterationOnly ) {
