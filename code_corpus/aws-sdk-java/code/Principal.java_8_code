    public Principal(String provider, String id) {
        this.provider = provider;
        if (provider.equals("AWS")) {
            id = id.replaceAll("-", "");
        }
        this.id = id;
    }

