    /**
     * How the device is exposed to the instance (e.g.,
     * <code>/dev/sdh</code>).
     *
     * @return How the device is exposed to the instance (e.g.,
     *         <code>/dev/sdh</code>).
     */
