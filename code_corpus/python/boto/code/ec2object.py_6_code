    def add_tag(self, key, value='', dry_run=False):
        
        status = self.connection.create_tags(
            [self.id],
            {key : value},
            dry_run=dry_run
        )
        if self.tags is None:
            self.tags = TagSet()
        self.tags[key] = value
