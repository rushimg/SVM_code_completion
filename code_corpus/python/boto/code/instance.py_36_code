    def get_attribute(self, attribute, dry_run=False):
        
        return self.connection.get_instance_attribute(
            self.id,
            attribute,
            dry_run=dry_run
        )
