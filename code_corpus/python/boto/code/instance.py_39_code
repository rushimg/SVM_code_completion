    def create_image(self, name, description=None, no_reboot=False,
                     dry_run=False):
        
        return self.connection.create_image(
            self.id,
            name,
            description,
            no_reboot,
            dry_run=dry_run
        )
