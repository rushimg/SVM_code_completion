    def create_snapshot(self, description=None, dry_run=False):
        
        return self.connection.create_snapshot(
            self.id,
            description,
            dry_run=dry_run
        )
