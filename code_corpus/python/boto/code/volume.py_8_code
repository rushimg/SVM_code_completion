    def attach(self, instance_id, device, dry_run=False):
        
        return self.connection.attach_volume(
            self.id,
            instance_id,
            device,
            dry_run=dry_run
        )
